#@ load("@ytt:data", "data")
#@ load("@ytt:base64", "base64")
apiVersion: v1
kind: Secret
metadata:
  name: #@ data.values.secret_name
stringData:
  gcp-service-account.json: #@ base64.decode(data.values.gcp_service_account_json)


